<UserControl x:Class="Notifier.PaveViews.TransportationSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:Notifier.PageViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:TransportSelectionViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
		<Button Grid.Column="0" MinWidth = "70" Content="БЧ" Command="{Binding RailwayCommand}"/>
		<Button Grid.Column="1" MinWidth = "70" Content="Bus Stolbtcy" Command="{Binding BusStolbtcyCommand}"/>
		<Button Grid.Column="2" MinWidth = "70" Content="Bus Atlas" Command="{Binding BusAtlasCommand}"/>
    </Grid>
</UserControl>
